name: "Terraform Azure Deployment"

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read
  pull-requests: write

jobs:
  terraform-pipeline:
    name: "Terraform Run"
    runs-on: ubuntu-latest
    
    # الصلاحيات القياسية لآشور المربوطة بالـ Secrets في الريبو بتاعك
    env:
      ARM_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID }}
      ARM_CLIENT_SECRET: ${{ secrets.ARM_CLIENT_SECRET }}
      ARM_SUBSCRIPTION_ID: ${{ secrets.ARM_SUBSCRIPTION_ID }}
      ARM_TENANT_ID: ${{ secrets.ARM_TENANT_ID }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.7.0"

      - name: Terraform Init
        run: terraform init

      # سيعمل الـ Plan فقط في حال وجود Pull Request
      - name: Terraform Plan
        if: github.event_name == 'pull_request'
        run: terraform plan -no-color

      # سيعمل الـ Apply فقط عند الـ Push (الدمج) في الـ main
      # تم إضافة -input=false لكسر أي تعليق أو طلب يدوّي فوراً
      - name: Terraform Apply
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: terraform apply -auto-approve -refresh=false -input=false
